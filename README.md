### Sending protobuf messages to kafka
1) Change kafka bootstrap address in `Main.scala`
2) Change kafka topic in `Main.scala`
3) Run `sbt run`

program will send several messages to topic, more information in code :)